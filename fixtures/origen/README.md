# origen — Synthetic ORIGEN TAPE samples authored for Nucleide (no license needed)

`tape5_sample` (input echo), `tape6_sample` (inventory), `tape9_sample` (decay constants).

The interactive activation tutorial embeds these samples as the defaults of
its ORIGEN TAPE5/TAPE6/TAPE9 tabs.
