# alara — UW ALARA samples vendored verbatim (terms in `alara/LICENSE.ALARA`)

Input-deck oracles `decks/sample2` + `decks/sample3`; group fluxes `flux/fluxin2`
plus all-zero edge case `flux/fluxin_zeros`; support libraries `libs/sampleMatlib`,
`libs/myElelib`, `libs/NRCA`, `libs/NRCC`; activation-output oracle `output/sample2.out`.
