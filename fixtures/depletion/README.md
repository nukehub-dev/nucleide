# depletion — Depletion-chain XML files (simple chains, Ni chain)

`chain_simple.xml` (staged for the website), `chain_simple_decay.xml`, `chain_ni.xml`.
`chain_abc.xml` is a synthetic A → B → C sequential-decay chain for time-series tests.
