# serpent — Serpent 1 & 2 res/dep/det outputs

`sample_*` (Serpent 1); `serp2_*` (Serpent 2).

The interactive serpent-io tutorial stages `serp2_res.m`, `sample2_dep.m`,
and `sample_det.m` via `npm run sync-data` (see `fixtures/README.md`).
